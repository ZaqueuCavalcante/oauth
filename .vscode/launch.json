{
    "version": "0.2.0",
    "compounds": [
        {
            "name": "RUN",
            "configurations": [
                "DrawApp",
            ],
            "preLaunchTask": "build",
            "stopAll": true
        }
    ],
    "configurations": [
        {
            "name": "DrawApp",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "requireExactSource": false,
            "cwd": "${workspaceFolder}/DrawApp",
            "program": "${workspaceFolder}/DrawApp/bin/Debug/net9.0/DrawApp.dll",
        }
    ]
}
